import 'package:flutter/material.dart';

class LecturerHomePage extends StatelessWidget {
  const LecturerHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Lecturer Home"), // ไปแก้เองนะจ๊ะ
      ),
      body: Container(),
    );
  }
}
